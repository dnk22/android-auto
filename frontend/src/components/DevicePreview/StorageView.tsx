export default function StorageView(): JSX.Element {
  return (
    <div className="flex min-h-[320px] flex-1 items-center justify-center rounded-3xl border border-dashed border-[var(--card-border)] bg-[var(--panel-soft)] px-5 text-center text-sm text-[var(--muted)]">
      Storage view is ready for the next feature set.
    </div>
  );
}
