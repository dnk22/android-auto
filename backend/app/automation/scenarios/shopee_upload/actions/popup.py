from __future__ import annotations

from typing import Any


async def dismiss_popups_if_any(connection: Any, auto_log_context=None) -> None:
    """Placeholder for closing Shopee popups if any."""
    _ = (connection, auto_log_context)
    return None
